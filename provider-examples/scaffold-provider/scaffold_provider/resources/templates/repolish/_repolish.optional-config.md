# Optional Configuration Notes

This `CONFIG.md` is **create-only**: it is seeded on the first `repolish apply`
after `include_optional_config: true` is set in `repolish.yaml`, and never
overwritten afterwards.  Customize freely.
