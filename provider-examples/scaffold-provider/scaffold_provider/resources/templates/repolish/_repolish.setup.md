# Setup Notes

This `SETUP.md` is **create-only**: `scaffold-provider` seeds it on the first
`repolish apply` and never overwrites it again.  Customize freely to document
project-specific setup instructions.
