providers:
  template_a:
    provider_root: ../template_a
    context_overrides:
      package_name: some-package

delete_files: []

anchors:
  extra-deps: |
    requests = "^5.30"
  install: |
    RUN apt-get update && apt-get install -y yq
