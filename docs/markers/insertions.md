# Insertions

`repolish:on:tag function args` … `repolish:off:tag` blocks in developer-owned
files get their bodies filled by provider functions — table of contents,
badge rows, generated headers. Marker edits (function, args) you make are
adopted back on every apply.

<!-- Align with: tests/insertions/ (parser, writer, adoption, files node), provider-development/insertions.md (provider side); decide how much moves here vs stays in Provider Anatomy -->
