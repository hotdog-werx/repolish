# Keep Blocks

`repolish-keep-block[name]: start="..." end="..."` (or `end-regex="..."`)
preserves a bounded region from your current file across regeneration —
including repeated marker pairs.

<!-- Align with: tests/directives/test_keep_processors.py (occurrence pairing, end-regex, indentation) -->
