# Regex

`repolish-regex[name]: <pattern>` captures one value from your current file
(first capture group, or the full match without groups) and injects it into
the template — versions you bumped, fields you edited.

<!-- Align with: tests/directives/test_processors.py regex cases, tests/directives/test_processors_helpers.py (indent trimming), concepts/preprocessors.md regex section (to migrate here) -->
