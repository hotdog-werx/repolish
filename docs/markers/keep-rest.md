# Keep the Rest

`repolish-keep-rest[name]: marker="..."` (aliases: `the-rest`, `footer`) keeps
everything from a marker to the end of the file — appended sections, local
additions, tooling blocks.

<!-- Align with: tests/directives/test_keep_processors.py rest cases -->
