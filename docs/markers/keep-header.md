# Keep the Header

`repolish-keep-header[name]: marker="..."` (alias: `the-header`) keeps the
file's head — everything before a marker — such as license banners or
hand-maintained imports.

<!-- Align with: tests/directives/test_keep_processors.py header cases -->
